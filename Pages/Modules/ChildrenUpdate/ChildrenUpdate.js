import React from 'react';
import {Footer, Button, Icon, FooterTab, Text, Tabs, Tab} from 'native-base';
import ScreenContainer from './../../../Navigation/ScreenContainer';

const ChildrenUpdate = ({navigation, route}) => {
  return (
    <ScreenContainer>
      <Text>ChildrenUpdate Page!</Text>
    </ScreenContainer>
  );
};

export default ChildrenUpdate;

