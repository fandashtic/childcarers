import React from 'react';
import {Footer, Button, Icon, FooterTab, Text, Tabs, Tab} from 'native-base';
import ScreenContainer from './../../../Navigation/ScreenContainer';

const LearningJourney = ({navigation, route}) => {
  return (
    <ScreenContainer>
      <Text>LearningJourney Page!</Text>
    </ScreenContainer>
  );
};

export default LearningJourney;
